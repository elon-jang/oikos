# 삭제 버튼을 52번 누르기 싫어서 벌어진 일

> "개발자는 게으르다. 그래서 자동화한다."

어느 평화로운 오후, 나는 QueryPie Knowledge 페이지를 바라보고 있었다. 52개의 테스트 데이터가 나를 조롱하듯 줄지어 있었다. 각각의 항목을 삭제하려면 **메뉴 클릭 → Delete Source 클릭 → 확인 버튼 클릭**. 무려 156번의 클릭이 필요했다.

"이걸 손으로 한다고?"

나는 Claude에게 물었다. "이거 자동화할 수 있어?"

---

## 1막: Playwright Codegen의 등장

Playwright에는 `codegen`이라는 마법 같은 기능이 있다. 브라우저에서 내가 하는 행동을 그대로 녹화해서 코드로 변환해준다.

```bash
npx playwright codegen https://example.com
```

브라우저가 열리고, 옆에 Inspector 창이 뜬다. 내가 클릭할 때마다 코드가 실시간으로 생성된다. 마치 매크로 녹화기 같지만, 결과물이 프로덕션 레벨의 테스트 코드다.

**문제는 로그인이었다.**

Google OAuth + Okta MFA. 이 조합은 자동화의 천적이다. Codegen으로 녹화하려면 먼저 로그인을 해야 하는데, 이미 Chrome이 켜져 있으면 프로필 충돌이 난다.

```
Error: Profile in use
```

"Chrome 닫으라고? 내 탭 30개를 포기하라고?"

---

## 2막: Firefox로의 탈출

해결책은 의외로 간단했다. **Firefox를 쓰면 된다.** Chrome은 일상용, Firefox는 자동화용. 완벽한 역할 분담이다.

```javascript
const { firefox } = require('playwright');
const browser = await firefox.launch({ headless: false });
```

그런데 여기서 한 가지 배운 점: **Playwright의 Firefox는 시스템 Firefox가 아니다.** Playwright는 자체적으로 브라우저를 다운로드해서 사용한다. 그래서 시스템 Firefox의 로그인 세션을 가져올 수 없다.

결국 수동 로그인은 피할 수 없었다. 하지만 괜찮다. 한 번만 로그인하면 세션을 저장할 수 있으니까.

---

## 3막: 로그인 감지의 기술

처음엔 "Enter 키를 누르면 시작"하는 방식을 시도했다. 하지만 stdin이 말을 안 듣는다. 터미널과 브라우저 자동화를 섞으면 이상한 일이 벌어진다.

**해결책: URL로 로그인 완료를 감지한다.**

```javascript
// 로그인 페이지로 리다이렉트되면 대기
const needsLogin = page.url().includes('login');

if (needsLogin) {
  console.log('⏳ 로그인을 진행하세요...');
  await page.waitForURL(targetUrl, { timeout: 300000 });

  // 로그인 성공! 세션 저장
  await context.storageState({ path: 'auth.json' });
}
```

OAuth 로그인은 여러 번 리다이렉트된다. Google → Okta → 다시 앱으로. `waitForURL`은 최종 목적지에 도착할 때까지 알아서 기다려준다.

---

## 4막: 삭제의 무한 루프

핵심 로직은 놀랍도록 단순하다:

```javascript
while (true) {
  const menuButtons = page.getByLabel('Open menu');
  if (await menuButtons.count() === 0) break;

  await menuButtons.first().click();
  await page.getByRole('menuitem', { name: 'Delete Source' }).click();
  await page.getByRole('button', { name: 'Delete' }).click();
}
```

`getByLabel`, `getByRole` - 이 접근성 기반 셀렉터들이 진짜 꿀이다. CSS 클래스는 언제든 바뀔 수 있지만, `aria-label="Open menu"`는 웬만하면 안 바뀐다. 스크린 리더 사용자를 위한 배려가 자동화 개발자에게도 축복이 된다.

---

## 에필로그: 52번의 클릭이 6줄의 코드가 되기까지

결과는 성공적이었다.

```
🗑️  1개 삭제됨
🗑️  2개 삭제됨
...
🗑️  52개 삭제됨

📊 총 52개 항목 삭제 완료
```

156번의 클릭이 `npm start` 한 번으로 바뀌었다. 다음에 또 테스트 데이터를 지워야 할 때, 나는 커피를 마시며 지켜보기만 하면 된다.

---

## TIL (Today I Learned)

1. **Playwright codegen**은 셀렉터 탐색의 최고 도구다
2. **Chrome 프로필 충돌**이 나면 Firefox로 도망가자
3. **OAuth 로그인 감지**는 URL 기반이 가장 안정적이다
4. **접근성 셀렉터**(`getByRole`, `getByLabel`)는 CSS보다 견고하다
5. **수동 + 자동 하이브리드**가 때로는 최선이다

---

> 코드는 GitHub에: [elon-jang/auto-delete](https://github.com/elon-jang/auto-delete)

*"게으름은 미덕이다. 단, 자동화로 이어질 때만."*
